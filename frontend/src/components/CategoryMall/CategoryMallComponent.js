import styled from 'styled-components';

export const CMContainer = styled.main`
    position: absolute;
    top: 15rem;
    left: 0;
    width: 100%;
    padding-top: 4rem;
`;

export const SliderWrapper = styled.section`
    height: 35rem;
`;
